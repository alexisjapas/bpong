[package]
name = "bpong"
version = "0.1.0"
edition = "2024"

[dependencies]
avian2d = "0.6.1"
bevy = "0.18.1"
rand = "0.10.0"

[profile.release_optimized]
inherits = "release"
lto = "fat"
codegen-units = 1
panic = "abort"

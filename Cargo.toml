[package]
name = "bpong"
version = "0.2.0"
edition = "2024"

[dependencies]
avian2d = "0.6.1"
bevy = "0.18.1"
bevy_embedded_assets = "0.15.0"
rand = "0.10.0"

[profile.release_optimized]
inherits = "release"
lto = "fat"
codegen-units = 1
panic = "abort"
